type paths = {
  home: string;
  dashboard: string;
  login: string;
  register: string;
  qrScanner: string;
};

export const PATHS: paths = {
  home: "/",
  dashboard: "/dashboard",
  qrScanner: "/qr-scanner",
  register: "/register",
  login: "/login",
};
