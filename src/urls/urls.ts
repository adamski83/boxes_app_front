type paths = {
  home: string;
  dashboard: string;
  invoices: string;
  login: string;
  register: string;
};

export const PATHS: paths = {
  home: "/",
  dashboard: "/dashboard",
  invoices: "/invoices",
  register: "/register",
  login: "/login",
};
