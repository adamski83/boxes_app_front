type paths = {
  home: string;
  dashboard: string;
  invoices: string;
  login: string;
};

export const PATHS: paths = {
  home: "/",
  dashboard: "/dashboard",
  invoices: "/invoices",
  login: "/login",
};
